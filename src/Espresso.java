public class Espresso {
  public static void main(String[] args) {
    // Check if args[0] exists; if not display an error and exit
    // Check if the file path it leads to is valid
    // Pass the file reference into the interpreter
  }
}